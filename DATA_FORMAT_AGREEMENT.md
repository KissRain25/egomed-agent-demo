# EgoMed 项目 —— 数据格式约定（分工交接文档）

> 用途：你和小王（队友）之间的唯一交接点。**只要这份约定两边都遵守，你俩就可以各自开工，互不等待。**

## 1. 分工一句话

| 人 | 负责 | 交接点 |
|----|------|--------|
| **小李（你）** | 数据 + 视频整理（EgoMed-Screen） | 按本约定把数据放到约定位置 |
| **小王（队友）** | 代码 + 跑通流程（EgoMed-Agent） | 按本约定读数据、跑通 pipeline |

---

## 2. 数据放哪（目录约定）

所有数据统一放在：

```
D:\BaiduNetdiskDownload\EgoMed-Screen\
```

每个**医学成像模态**一个文件夹，共 5 个：

| 文件夹 | 模态 | 对应医学设备 |
|--------|------|-------------|
| `ACDC/` | MRI（磁共振） | 心脏 MRI |
| `Amos/` | CT | 腹部 CT |
| `CAMUS/` | 超声 | 心脏超声 |
| `Montgomery-County-CXR-Set/` | X光 | 胸片 |
| `PolypGen2021_MultiCenterData_v3/` | 内窥镜 | 肠镜 |

---

## 3. 每个模态内部的标准结构（以 ACDC 为例）

```
ACDC/
├── rawdata/            # 【原始视频】第一人称操作录像，MP4
│   └── VID_xxxx.mp4
├── screen/             # 【屏幕帧】从视频抽出的每一帧截图，PNG
│   └── *.png
├── dicom/              # 【医学原图】DICOM 格式，.dcm
├── label/              # 【标注】病变/区域标注，PNG
├── rawlabel_png_gray/  # 【灰度标注】二值掩码，PNG
└── info/               # 【元信息】文本说明，.txt
```

> 其他 4 个模态文件夹**结构可能略有不同**，但核心三类必须对齐：
> **① rawdata（原始视频） ② screen（屏幕帧） ③ label（标注/掩码）**

---

## 4. 命名规范（重要）

- **视频**：统一 `VID_日期_时间.mp4`，如 `VID_20260423112257.mp4`
  - 不要用中文名、不要带空格、不要带特殊符号。
- **图片帧**：用 `患者ID_序号.png`，如 `patient01_001.png`
- **标注掩码**：与图片帧**同名**，放在 `label/` 里，如 `patient01_001.png`
  - 图片和掩码**一一对应、名字必须一致**，代码才能对上号。
- **禁止**：中文路径、空格、括号 `()`、`#` 等符号。

---

## 5. 交接检查清单（每次传数据前对照）

- [ ] 5 个模态文件夹都在 `EgoMed-Screen/` 下
- [ ] 每个模态都有 `rawdata/`（原始视频 MP4）
- [ ] 每个模态都有 `screen/`（屏幕帧 PNG）
- [ ] 每个模态都有 `label/`（标注 PNG，与帧一一对应、同名）
- [ ] 所有文件名无中文、无空格、无特殊符号
- [ ] 命名格式统一（`VID_日期_时间.mp4` / `患者ID_序号.png`）

---

## 6. 数据更新流程（避免冲突）

1. 你整理好数据后，传到**共用的百度网盘链接**。
2. 在网盘里用**文件夹区分人**：如 `小李_数据/` 和 `小王_代码/`，互不覆盖。
3. 更新数据时，**只动你自己的文件夹**，并在文件名里带日期（如 `data_20260828`），不要覆盖旧版，留个备份。

---

## 7. 一句话总结

> **你管 `EgoMed-Screen`（数据），小王管 `EgoMed-Agent`（代码）；你俩靠这份"数据格式约定"对接，各自开工，互不等。**
