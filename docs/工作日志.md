# 工作日志（会话 → 文档落盘）

> 用途：AI 对话是易失的，本文件是**跨会话的断点记录**。
> 规则：每完成一段工作追加一条（日期 + 做了什么 + 下一步）；开新会话时让 AI 先读本文件 + `PROGRESS.md` 即可恢复全部上下文。
> 详细的字段/契约问题永远查 `API_CONTRACT.md`（v1.0 已冻结），分工/流程查 `接口.md`，项目大事记查 `PROGRESS.md`。

---

## 2026-09-16（ASR 谐音 + T5 + 验收脚本）

- **ASR 谐音解决**：`scripts/demo/asr_medical.py`（medium + zh + 领域 prompt + hotwords + beam5 + VAD），中文医学词汇 1/8 → 8/8 全对，稳态 400~500ms/句；契约 v0.2.3（A.11）
- **T5 客户端闭环**：`client/mock_client.py` 压缩自测 17/17、假实现联调 5/5、结果图落盘
- **一键验收脚本**：`client/acceptance_test.py`（复用 mock_client 逻辑，mock/real 双套件，报告落盘 `runs/acceptance/`）；修掉 R3 断言 bug（并发耗时含排队等待，改为串行基准对比）
- **联调发现 3 处文档矛盾** → 裁决 v0.2.2，产出 A.10 差异清单（P0×4 / P1×4 / P2×3），发返工通知给李

## 2026-09-18（P0 返工复验 + T6/T7 闭环）

- 李交付返工版 `server/api.py` + `asr_medical.py` 接入，复验全过：P0.1 并发 8.75s→238ms、P0.2 首帧 66s→42ms、P0.3/P0.4 坏帧 5/5 返空、ASR 7/8
- **known issue（不阻塞）**：cmd_02.wav 偶发 whisper 幻觉复读（9~20s + 乱码），脱离服务端可复现，**归闫加复读检测防护，尚未做**
- 契约 v0.2.4、接口.md v0.3.2、PROGRESS.md 第 13 项；复验回执已发李

## 2026-09-19（T8 冻结 + 持久化机制建立）

- **T8 方案冻结完成**：`API_CONTRACT.md` → v1.0（冻结版存档 `docs/API_CONTRACT_v1.0_frozen.md`），字段/路径/错误码锁死，变更走 v1.x 流程；接口阶段 T1~T8 收官
- **协作模式确认**：李只按指令行动，不做开放式询问；接口变更一律由闫单点发起
- **待导师确认 4 项**（眼镜型号/服务器位置/回显介质/数据合规）始终未定，开发按暂定假设走
- 建立本日志 + AI 持久记忆，解决"对话无法存留太久"问题
- **建总计划 `docs/总计划.md`**（N1~N12 任务表，三阶段推进）
- **找回导师任务原文并建 `docs/端到端流程设计.md` v0.1**：导师三件事（代码复现✅/模拟做软件◐/流程设计📝）逐项对照，端到端三层架构（眼镜端零智能纯采集+回显，服务器全处理）、模拟策略（摄像头/窗口替代眼镜，到位后只换适配层）、4 个开放问题；**眼镜尚未购买**，N3 更新为◐
- **全部成果已推 GitHub**（`KissRain25/egomed-agent-demo`）：两个提交 `1267efc` feat（ASR/服务端/验收脚本）+ `f232fe9` docs（v1.0 冻结+统筹文档）；`.gitignore` 补排除 `*.bak-*` 与 `run_test.py`
- **建 `docs/AI协作规范.md`（v1.0）**：三原则（文档即真相/改动必绿/最小授权）、开局四读、8 条红线（不改接口字段、不改 server/**、不加依赖、不改现有引擎、不删数据、不跑破坏性 git、不提交大文件、不放宽验收）、标准流程七步、落盘分层表、汇报格式、6 类反模式
- **建 `docs/决策记录.md`（ADR）**：补记 D1~D8 设计决策（契约先行冻结/眼镜端零智能/只做 A1A2/坏帧返空/丢帧 latest-wins/ASR 优化必配防护/模拟替代眼镜/单文件单负责人+指令式派活）
- **N1 完成：ASR 幻觉防护**（契约 A.11.1 / v1.0.1）：`asr_medical.py` 三层防护（① 解码侧 `condition_on_previous_text=False`+repetition_penalty+no_repeat_ngram_size，不支持的参数自动剔除；② 输出侧 2~4-gram 重复率≥0.5 / 连续同字≥4 / 字符超时长比例；③ 惰性消费 segments 计时，首次 4s、重试 2.5s 上限）+ 降级重试（去 hotwords/prompt，beam1）+ 返空串兜底；`transcribe` 签名不变、服务端零改动。**实测**：模块自测 25/25（8 句 **8/8**，此前 7/8；cmd_02 **8.8s→1.2s** 且文本正确、连跑 10 次全对），端到端 real 套件 **10/10**（R1 49ms、语音最大 1224ms、R3 389ms，报告 `runs/acceptance/20260919_233542`）。新增 CLI `--only/--stress` + 检测器单元用例。**阶段一仅剩 N3（问导师 4 项）**

## 2026-09-20（协作规范 + N4 子计划）

- **建 `docs/AI协作规范.md`**（v1.0，推送 `570310c`；v1.0.1 `f1fcb2e` 放宽红线 2：`server/**` 经闫授权可改）
- **建 `docs/决策记录.md`**：D1~D8 设计决策
- **建 `docs/子计划_N4_摄像头实时采集.md`（v1.0）**：M1~M5 验收标准（视频源自动跑通≥5fps / 真摄像头 720p 实时分割 / 防抖不闪跳 / 语音交互 / 报告落盘）、N4.1~N4.6 分步、关键设计决策（发帧 5fps、后台发送线程并发=1 丢旧帧、空 overlay 保留上一帧、坏帧判定以服务端为准、FrameSource 抽象）、7 项风险对策、CLI 设计
- **总计划新增"子计划（分支）"章节**：命名规则 `子计划_N<编号>_<短名称>.md` + 模板 12 要素；N4 状态 ◐ 待开工
- 待授权：① `sounddevice`（真麦克风，N4.6）② M1 视频源素材（无 mp4 则用样例帧序列）

## 2026-09-20（下）：N4 客户端交付 + 真引擎联调发现

- **N4.1~N4.4 完成：`client/live_client.py`**（FrameSource 抽象 video/cam/glasses占位、抽帧发送后台线程并发1覆盖、HUD 中文 PIL 绘制、防抖沿用上帧、`--probe`/`--auto` 报告、`--audio` 先设目标、`--expect-overlay` 断言）。实测：帧目录源 133 帧/发 30/丢 0/4.34fps（假实现）；抽样帧读图确认 HUD 与叠加正常
- **依赖 `sounddevice 0.5.6` 已装**（决策 D9），麦克风设备 Realtek 阵列可用；N4.6 代码待写
- **重大发现（建 `docs/联调发现_20260920_真引擎实时链路.md`）**：
  1. 真引擎分割正常（X光左肺 overlay 1.2MB，第2帧起）
  2. **首帧必返空**：服务端 `_segment_target` 用默认 `min_occurrence=2`，单帧必被过滤 → N13
  3. **A1 延迟随窗口增长**：2帧 1.36s → 6帧 3.39s（WINDOW_SIZE=10 全窗口重跑），突破客户端 3s 超时，HTTP 超时 7 次 → N13
  4. **ACDC / PolypGen 原始 YOLO 零检出**（conf 0.05、8 种参数组合全 0），其余三模态正常 → N14
  5. 我方 `acceptance_test.py` real 套件缺 overlay 断言（掩盖了 2/4）→ N15；live_client 侧已补 `--expect-overlay`
- 总计划新增 N13/N14/N15；子计划 N4 → v1.2（N4.5/N4.6 待做，N4.5 建议在 N13 后做）

## 下一步（按优先级）

**统一看 `docs/总计划.md`（N1~N12 任务表）**，此处只记当前断点：
- 当前活跃：阶段一（N1 cmd_02 幻觉防护 / N3 待导师确认 4 项），N2 已完成
- 下一阶段：N4 摄像头实时采集（`client/live_client.py`，复用 mock_client 发帧逻辑）
