# EgoMed-Agent 总计划（v1.0 冻结后路线图）

> 创建：2026-09-19 ｜ 维护：闫 ｜ 配套：断点看 `工作日志.md`，大事记看 `PROGRESS.md`，接口规则看 `API_CONTRACT.md`（v1.0 冻结），端到端流程与导师任务对照看 `端到端流程设计.md`，**与 AI 的协作规则看 `AI协作规范.md`，设计取舍看 `决策记录.md`**
> 用法：任务全编号（N1~N12），干一件勾一件；给李的活由闫在此计划里挑出后写成具体指令下发（李只按指令行动）。
> 导师任务（2026-09-19 找回）：①代码复现 ✅ ②模拟情况做软件（本计划阶段二）③设计眼镜→服务器流程（`端到端流程设计.md` v0.1 已建）。**眼镜尚未购买，全模拟策略推进，不阻塞**。

---

## 当前基线（2026-09-19）

- **T1~T8 接口阶段收官**：契约 v1.0 冻结，mock 8/8、real 9/10（唯一 FAIL = cmd_02 known issue，不阻塞）
- 全链路可跑：语音（中文医学词 7/8）→ NLU → 5 模态识别 → YOLO → SAM2 分割 → 结果回显
- 性能：首帧 5~42ms（预热后）、并发 5 帧 238ms、语音 400~700ms/句
- 环境：`egomed`（Python 3.12 + torch 2.8 + RTX 5060），解释器 `D:\ScienceApp\anaconda\envs\egomed\python.exe`

---

## 阶段一：收尾清零（本周，闫独立完成，不依赖李）

| # | 任务 | 内容 | 验收标准 | 状态 |
|---|------|------|----------|------|
| N1 | **cmd_02 幻觉防护** | `asr_medical.py` 三层防护（解码侧抑制/复读检测/时间兜底）+ 降级重试 + 返空串兜底；A.11.1 | cmd_02 连跑 10 次全对、单次 <3s；全套件 8/8 全绿 | ☑ 2026-09-19（8/8，cmd_02 8.8s→1.2s，real 套件 10/10） |
| N2 | **P0 收尾** | T8 冻结落盘 + 群通知李（v1.0 生效） | 文档齐 + 李知情 | ☑ 2026-09-19 |
| N3 | **待导师确认 4 项** | 眼镜型号（**已知未购买**）/ 服务器位置 / 回显介质 / 数据合规——找导师要答案，回来更新契约假设与 `端到端流程设计.md` 第五节 | 4 项有明确答复并登记 | ◐ 眼镜未购已确认 |

## 阶段二：实时化改造（下周起，项目从"选文件"变"看视频流"）

| # | 任务 | 内容 | 验收标准 | 状态 |
|---|------|------|----------|------|
| N4 | **摄像头实时采集** | 新建 `client/live_client.py`：摄像头回调 → 压缩（复用 mock_client 逻辑）→ `post_frame`；接口不变，v1.0 冻结无影响 | 摄像头对准样例画面，实时出分割结果 ≥5 fps 有效帧 | ☐ |
| N5 | **本地回显 + 防抖** | 回显窗口（OpenCV 即可）；"空 overlay 保留上一帧"防抖分支（P0.3 修复后首次实战验收） | 转头/遮挡时画面不闪跳，出提示语 | ☐ |
| N6 | **窗口模式降延迟** | 服务端改滑动窗口推理（P1.8），单帧 6.5s → 目标 ~2s；**李执行，闫写具体指令** | 单帧 P95 < 2500ms | ☐ |
| N7 | （可选）WS 推送 | 契约 A4 预留的 `WS /api/v1/ws/{session_id}`，视 N4~N6 实测延迟再决定做不做 | — | ☐ |

## 阶段三：成果化（论文/答辩素材，阶段二完成后并行推进）

| # | 任务 | 内容 | 验收标准 | 状态 |
|---|------|------|----------|------|
| N8 | **量化指标** | 分割精度（IoU/Dice vs 数据集标注）；ASR 字准率（8 句扩到 50 句）；端到端延迟分布（P50/P95） | 一张指标总表 + 原始数据存档 `runs/metrics/` | ☐ |
| N9 | **失败案例分析** | 坏帧拒识行为、NLU 歧义样本、模态切换抖动、cmd_02 幻觉——每个都有"现象→根因→对策" | ≥4 个案例成文，可作消融/讨论素材 | ☐ |
| N10 | **演示视频** | 录屏：语音指令 → 眼镜/摄像头画面 → 实时分割全程 | 1~2 分钟无声卡点、可交导师 | ☐ |
| N11 | **A.10 P1/P2 backlog 处置** | P1×4（2003 触发路径等）/ P2×3 按需挑项给李，不阻塞主线 | 挑中的走指令下发，其余挂 v1.x | ☐ |
| N12 | **导师评审** | 阶段 demo + 指标汇报，按 N3 的答复调整方向 | 导师反馈登记入档 | ☐ |

---

## 依赖与顺序

```
N1（独立）──┐
N3（独立）──┼──→ N4 → N5 →（N6 与 N4/N5 并行，李侧）→（N7 视实测）→ N8 → N9 → N10 → N12
            │                                                              ↑
            └── N11 随时按需插入 ────────────────────────────────────────────┘
```

**关键原则**：接口已冻结 v1.0——阶段二/三所有开发**只允许动 client/ 和新增文件**，需动接口的走 v1.x 流程（提出→确认→更新契约→群通知）。

## 每周节奏（建议）

- 周初：从本计划挑 1~2 个 N 项，写进工作日志"本周目标"
- 干完即勾状态 + 工作日志追加一条
- 涉及李的活：单写指令文件/群消息，明确"改什么文件、验收跑什么命令"
