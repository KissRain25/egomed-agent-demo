# EgoMed 分割 Demo —— 队友安装使用说明

> 这是一份给队友看的傻瓜式教程。跟着做，就能在自己电脑上独立使用这个系统。

## 这套系统是什么？

一个"说句话就能分割医学影像"的网页工具。支持 **5 种影像**：心脏 MRI、腹部 CT、心脏超声、胸片 X 光、内窥镜。
你说"帮我分割左心室"，它就在视频里自动找到并描出来。

---

## 第 0 步：准备（一次性）

1. 一台 **Windows 电脑**（最好有 NVIDIA 显卡，没有也能跑但比较慢）
2. 安装 **Anaconda**（Python 环境管理工具）
   - 下载：https://www.anaconda.com/download
   - 安装时一路"下一步"即可（勾上 Add to PATH 也行）

## 第 1 步：拿到代码（二选一）

- **方式 A（推荐）**：让发给你这份文档的人，把整个项目文件夹压缩发给你，解压到任意位置，比如 `D:\egomed-agent-demo`
- **方式 B**：代码已推到 GitHub，用命令拉取（需能访问 GitHub）：
  ```
  git clone https://github.com/KissRain25/egomed-agent-demo.git
  ```

## 第 2 步：一键安装环境（最省事）

1. 双击项目文件夹里的 **`安装环境.bat`**
2. 它会自动完成三件事（**耗时 10~30 分钟，请耐心等，期间电脑别睡觉**）：
   - 创建 Python 3.12 环境（egomed）
   - 安装全部依赖包（PyTorch、SAM2 等）
   - 下载 5 种影像的模型权重（走国内镜像，约 250MB）
3. 看到"安装完成"就 OK

> 如果中途报错：把窗口里的文字截图发给队友。

## 第 3 步：启动

1. 双击 **`启动EgoMedDemo.bat`**
2. 等 1~2 分钟（模型加载），窗口出现 `http://127.0.0.1:7860` 时自动打开浏览器
3. 网页使用：
   - 选 **模态**（比如"CAMUS (超声 心脏)"）
   - 填 **视频/图片路径**：填**你自己电脑上的**影像文件夹或视频文件路径（比如 `D:\我的数据\心脏超声\10`）
   - 输入指令，比如：**"分割左心室"** 或 **"帮我分割肝脏"**
   - 点开始，等结果视频

> 📌 注意：代码仓库里**不包含样例数据**。如果手上没有影像数据，可以问发给你文档的人要一份样例。

## 常见问题

| 问题 | 解决办法 |
|---|---|
| 启动时提示 `Cannot find empty port` | 服务已经在跑，直接打开 `http://127.0.0.1:7860` 即可 |
| 没有 NVIDIA 显卡 / 很卡 | 能跑但慢，属正常现象 |
| 下载权重失败 | 重新运行 `python scripts\download_models.py` |
| 打不开网页 | 看日志文件 `runs\gradio_log.txt` 最后几行，截图发给作者 |
| 装依赖时卡在 GitHub 下载（sam2 / CLIP）很久 | 国内网络连 GitHub 慢是正常的，多等一会儿；实在装不上就重跑一次 `安装环境.bat`，或联系作者要离线解决办法 |

## 有问题找谁

- 遇到任何报错，把窗口/网页里的错误文字截图，发给发给你这份文档的人。
